# Later Ideas

## Public listing view counts

Add a visible click/view count to each property listing so visitors can see listing interest.

- Deferred because accurate totals require shared storage rather than browser-only counting.
- Do not implement with a paid service.
- Revisit only when a reliable, confirmed free option is available.
- A browser-only `localStorage` counter is not suitable because it would show different totals on every device.
